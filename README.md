# Scholarly Review Room updates

This public repository contains only the signed Sparkle update feed and downloadable update ZIP for Scholarly Review Room. The application source and full release record remain in the private `grant-review-bot` repository.

The feed is maintained automatically by that project's `npm run publish:mac` command. Development happens directly on `main`; this update repository does not use feature branches or pull requests.
